# Workflow

1. Pull latest changes before starting work
2. Make changes
3. Commit and push

```sh
git pull --rebase
# ... make changes ...
git add -A
git commit -m "description of changes"
git push
```
